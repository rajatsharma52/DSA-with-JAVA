package Arrays;

public class length_of_an_array {

    public static void main(String[] args) {
        
        int marks[]= new int[100];

        // If I want to find the length of this array.
        // There is a special member (name of array.length) which is used to find the length of an array.

        System.out.println("The length of marks array is: " + marks.length);
    }
    
}
